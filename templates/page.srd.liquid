<!-- /templates/page.liquid -->
<div class="wrapper page-margin">
  <div class="grid">
    <div class="grid__item large--one-whole">
      <h1 class="text-center h2">{{ page.title }}</h1>
      <div class="rte">
        {{ page.content }}
        {% include 'addthis' %}
		{% section 'minisrdfooter' %}
      </div>
    </div>
  </div>
</div>
